[tool.poetry]
name = "bot_cron_job_eme"
version = "0.1.0"
description = ""
authors = ["DatBe <datbe26092001@gmail.com>"]
packages = [{ include = "bot_cron_job_eme" }]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.104.1"
uvicorn = "0.22.0"
pydantic-settings = "2.7.1"
celery = "^5.4.0"
python-dotenv = "^1.0.1"
redis = "^5.2.1"


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
